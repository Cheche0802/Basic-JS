aqui se tomaran las notas de las clases

clase n~33
cuando se trabaja con datos primitivos se pasan por valor
y todos los objetos son pasados por referencia



clas n° 34 
en la condicion se envia es un valor booleano
con un "=" es una asignacion
el == es para validar el valor pero no me importa el tipo de variable
con un "===" se valida el valor y la variable si son exactamente iguales


clase n° 36
el operador "and" se puede utilizar para disparar acciones unicamente si la primera condicion se cumple
Si la primera condicion boleana retorna el valor de "false" cierra el flujo 
si la primera condicion devuelve "true", sigue ejecutando lo demas del if
para el operador "or" solo se debe cumplir una sola condicion es todo lo contrario al operador "AND" que se debe cumplir ambas


clase 37
asignaciones
cuando se declara una contante y se le asigna varios valores con el operador "&&" este toma el ultimo valor
siempre y cuando el primer valor sea "true " de lo contrario no lo realizara


clase 38 
operador ternario

clase 39
Protip operador ternario

clase 40
switch

clase 41 
while y do while

clase 42
for

